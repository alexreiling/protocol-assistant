{
  "name": "main",
  "version": "1.0.0",
  "description": "Electron main process",
  "main": "index.js",
  "scripts": {
    "start": "nodemon --watch ./src/**/* --watch . --exec electron src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Reiling",
  "license": "ISC",
  "dependencies": {
    "electron": "^3.0.10",
    "electron-reload": "^1.3.0",
    "nodemon": "^1.18.9"
  }
}
